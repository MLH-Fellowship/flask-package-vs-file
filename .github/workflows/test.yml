name: Test secret
on: [push]

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Test secret
        env:
          ENV_SECRET: ${{ secrets.TestSecret }}
        run:
          echo $ENV_SECRET
